// JS - TS
// string, number, boolean, undefined, null

// TS: never, unknown, void

let userName: string = 'afique';

let userId = 123;

// userName.toFixed();
// userId.toFixed(2);
console.log(userId)
